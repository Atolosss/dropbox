patch:
  tags:
    - user
  summary: Patch user
  description: Patch User in bd
  operationId: patchUser
  parameters:
    - name: id
      in: path
      description: 'The id that needs to be fetched. Use 123 for example. '
      required: true
      schema:
        type: integer
      example: 657
  requestBody:
    description: Patch user object
    content:
      '*/*':
        schema:
          $ref: '../schemas/user/UserPatchRq.yml'
    required: true
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: '../schemas/user/UserPatchRs.yml'
    500:
      description: Something went wrong
      content:
        application/json: { }
delete:
  tags:
    - user
  summary: Delete user
  description: Delete User in bd
  operationId: deleteUser
  parameters:
    - name: id
      in: path
      description: 'The id that needs to be fetched. Use 123 for example. '
      required: true
      schema:
        type: integer
      example: 6578
  responses:
    200:
      description: Success
      content:
        application/json: { }
    500:
      description: Something went wrong
      content:
        application/json: { }
get:
  tags:
    - user
  summary: Get user
  description: Get User in bd
  operationId: getUser
  parameters:
    - name: id
      in: path
      description: 'The id that needs to be fetched. Use 123 for example. '
      required: true
      schema:
        type: integer
      example: 65787
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: '../schemas/user/UserRs.yml'
    500:
      description: Something went wrong
      content:
        application/json: { }
